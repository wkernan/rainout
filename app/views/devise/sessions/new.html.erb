<div id="modal-edit-fields">
	<div class="modal-dialog">
		<div class="form-content">
  			<div class="modal-header">
    			<h2 class="modal-title" id="myModalLabel">Sign In</h2>
  			</div>
  			<div class="modal-body">
    			<div class="small-container">
      				<%= simple_form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>

						<%= f.input :email, :placeholder => 'Email address', :autofocus => true %>
						<%= f.input :password, :placeholder => 'Password' %>

	  					<% if devise_mapping.rememberable? -%>
	    					<div><%= f.check_box :remember_me %> <%= f.label :remember_me %></div>
	  					<% end -%>

	  					<div class="center" style="margin-top: 30px; margin-right: 50px; margin-bottom: 40px;">
        					<%= f.submit "Sign in", class: "btn btn-primary" %>
     		 			</div>
					<% end %>
  	  			</div>
 	 		</div>
		</div><!-- /.modal-content -->
	</div><!-- /.modal-dialog -->
</div>
<%= render "devise/shared/links" %>
